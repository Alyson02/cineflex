const { default: styled } = require("styled-components");

const FilmeWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 11px 30px;
`;

export default FilmeWrapper;
